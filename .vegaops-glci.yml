
# job:
#   name: curl -L https://web --output web.html

# # Create a artifacts
# job:
#   artifacts:
#     name: "$CI_JOB_NAME"
#     paths:
#       - binaries/


image: curl

stages:
  - build

build:
  stage: build
  script:
    - curl -o web.html http://3.95.204.177
  artifacts:
    paths:
      - web.html

